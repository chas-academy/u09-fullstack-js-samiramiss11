{
    "name": "neuro-nexus-app",
    "version": "1.0.0",
    "engines": {
        "node": "18.x",
        "npm": "8.x"
    },
    "scripts": {
        "start": "npm run start --prefix server",
        "heroku-postbuild": "npm install --prefix bookland && npm run build --prefix bookland"
    },
    "devDependencies": {
        "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
    }
}
